//styles
import { StyleSheet } from 'react-native';

export const gStyles = StyleSheet.create({
	container: { flex: 1, alignItems: 'center' },
	bgImage: {
		flex: 1,
		resizeMode: 'contain',
		width: '100%',
	},
});

export const colors = { pink: '#C8B1E1' };
